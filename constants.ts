import { BigNumber } from "ethers";

export const ZERO = BigNumber.from(0).toHexString();
export const ONE = BigNumber.from(1).toHexString();
export const SWAP_AMOUNT = BigNumber.from(2).toHexString();
export const LEFT_CHAIN_ID = 66;
export const RIGHT_CHAIN_ID = 99;
